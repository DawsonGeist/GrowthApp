import * as React from 'react';
import { Button, View, Text } from 'react-native';

const ProfileHeader = ({ navigation }) => {
  return (
    <View>
        <Text style={{color: 'white', padding: 5, fontSize: 22}}>Edit Profile</Text>
    </View>
  );
};

export default ProfileHeader;